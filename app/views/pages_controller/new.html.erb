<%=link_to('Back to list', pages_controller_index_path, :class => 'back-line') %>
<div class="page new">
  <h2>Create Page</h2>
    <%=form_for(@page, :url => pages_controller_index_path) do |f| %>
      <%= render(:partial => 'form', :locals => {:f => f})%>
      <div class="form-buttons">
        <%= f.submit("Create Subject")%>
      </div>
    <% end %>
</div>
